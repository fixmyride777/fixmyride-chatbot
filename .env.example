ANTHROPIC_API_KEY=
WASENDER_API_TOKEN=
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Optional: debounce inbound burst (ms) to reduce duplicate replies
# PHONE_MESSAGE_DEBOUNCE_MS=600

# Optional (dev only): true = new UUID prefix each restart → chat memory does not persist (default: false)
# DEV_SESSION_ISOLATION=true
